<view class="container">
  <view class="form-group">
    <view class="label">任务标题</view>
    <input class="input" placeholder="输入任务标题 (如：食堂带饭)" bindinput="inputChange" data-field="title" value="{{title}}"/>
  </view>

  <view class="form-group">
    <view class="label">任务类型</view>
    <picker bindchange="bindTypeChange" value="{{typeIndex}}" range="{{types}}">
      <view class="picker">{{types[typeIndex]}}</view>
    </picker>
  </view>

  <view class="form-group">
    <view class="label">详细描述</view>
    <textarea class="textarea" placeholder="描述具体任务内容（注：本平台严禁发布课程辅导、家教代课等信息，请使用时间互助等中性词）" bindinput="inputChange" data-field="description" value="{{description}}"></textarea>
  </view>

  <view class="form-group">
    <view class="label">约定时间</view>
    <picker mode="date" value="{{time_range}}" start="{{today}}" bindchange="bindDateChange">
      <view class="picker">
        {{time_range ? time_range : '点击选择日期'}}
      </view>
    </picker>
  </view>

  <view class="form-group">
    <view class="label">约定地点</view>
    <input class="input" placeholder="输入交接地点" bindinput="inputChange" data-field="location" value="{{location}}"/>
  </view>

  <view class="form-group">
    <view class="label">悬赏金额 (元)</view>
    <input class="input" type="digit" placeholder="输入发布价格，将直接扣减余额" bindinput="inputChange" data-field="price" value="{{price}}"/>
  </view>

  <view class="form-group">
    <view class="label">担保金要求 (元)</view>
    <input class="input" type="digit" placeholder="接单者需冻结的担保金额，可为0" bindinput="inputChange" data-field="deposit" value="{{deposit}}"/>
  </view>

  <view class="form-group">
    <view class="label">接单信誉门槛 (星级/分)</view>
    <input class="input" type="number" placeholder="默认100分(1星)，可要求更高分" bindinput="inputChange" data-field="min_credit" value="{{min_credit}}"/>
  </view>

  <view class="tips">
    提示：发布任务将从您的钱包直接扣除【悬赏金额】。如有人接单后违约，您可获赔对方部分担保金；如无人接单或您主动取消，金额将全额退回钱包。
  </view>

  <button class="submit-btn" bindtap="submitTask">发布任务</button>
</view>